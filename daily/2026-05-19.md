# Daily Check-in — 2026-05-19

## 1) What I learned today
- NFT 合约的 `mint()` 里如果使用 `_safeMint` 触发外部回调（`onERC721Received`），在状态更新不完整时可能引入**重入风险**。
- 这类问题的修复思路通常是：**Checks-Effects-Interactions** 或加 `ReentrancyGuard`。

## 2) What I built / tried (if any)
- 沉淀 1 条审计条目（High）：`mint()` 缺少重入保护导致可超发。

## 3) Questions / confusions
- 这一类问题在 NFT 项目里最常见的“状态变量”更新点是什么（`_nextTokenId` / `totalSupply` / 退款逻辑等）？

## 4) Next action (tomorrow)
- 在 `tasks/week1-mvp.md` 里补全 Threat model（先选 2 个优先威胁）。
- 收集 3–5 个 NFT 常见漏洞 snippet，放进 `experiments/snippets/`。

## 5) References
- Audit finding: tasks/audit-findings/2026-05-19_missing-reentrancy-in-mint.md
- SWC-107
